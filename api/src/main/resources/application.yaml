spring:
  application:
    name: api
  config:
    import: optional:application-local.yaml
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      platform: postgresql
